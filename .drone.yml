---
kind: pipeline
type: ssh
name: ccc-ssh

server:
  host:
    from_secret: proxy-host
  user:
    from_secret: proxy-user
  ssh_key:
    from_secret: nabilah
  
steps:
- name: super-peer
  image: node
  commands:
  - node --version
  - docker ps

trigger:
  branch:
  - master
